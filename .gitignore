# Cloudflare Pages publica TODO lo que hay en el repo. Lo que no es parte del
# sitio no debe ir al repo o queda en alphacoresolutions.co/<archivo> a la vista
# de cualquiera (paso el 1-sep-2026: CLAUDE.md, con el precio de cierre interno,
# se servía público).
CLAUDE.md
setup-surface.bat
.claude/
